import Link from "next/link";import {LockKeyhole} from "lucide-react";import {LoginButton} from "./LoginButton";import {supabaseIsConfigured} from "@/lib/supabase/server";
export default function LoginPage(){return <section className="login-page"><div className="login-brand"><span className="eyebrow">N.T. LEAGUE ADMIN</span><h1>League control,<br/>kept private.</h1><p>Manage contestants, challenge dates, results and lessons.</p></div><div className="login-card"><LockKeyhole size={32}/><h2>Administrator sign in</h2><p>Only the approved Google account can enter.</p>{supabaseIsConfigured()?<LoginButton/>:<div className="setup-warning">Add the Supabase settings in Vercel first.</div>}<small>Approved: marcneizha@gmail.com</small><Link href="/">← Back to rankings</Link></div></section>}
