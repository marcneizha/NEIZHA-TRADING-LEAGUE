import {redirect} from "next/navigation";import {createSupabaseServerClient,supabaseIsConfigured} from "@/lib/supabase/server";import {addContestant,addContent,removeContestant,saveChallenge,signOut,updateContestant} from "./actions";
const ADMIN=process.env.ADMIN_EMAIL||"marcneizha@gmail.com";
export default async function Admin(){if(!supabaseIsConfigured())redirect("/auth/login");const s=await createSupabaseServerClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect("/auth/login");if(user.email?.toLowerCase()!==ADMIN.toLowerCase())return <section className="denied"><h1>Access denied</h1><p>{user.email} is not approved.</p><form action={signOut}><button>Sign out</button></form></section>;const{data:list}=await s.from("contestants").select("*").order("created_at");return <section className="admin-workspace"><header className="admin-heading"><div><span className="eyebrow">SECURE ADMIN</span><h1>League control room</h1><p>Signed in as {user.email}</p></div><form action={signOut}><button className="secondary-button">Sign out</button></form></header><div className="admin-columns"><div className="admin-stack"><article className="admin-card"><h2>Add contestant</h2><form action={addContestant} className="admin-form"><label>Name<input name="name" required/></label><label>Broker<input name="broker" required/></label><div className="form-row"><label>Starting balance<input name="starting_balance" type="number" step=".01" required/></label><label>Current balance<input name="current_balance" type="number" step=".01" required/></label></div><label>Status<select name="status"><option>Active</option><option>Completed</option><option>Eliminated</option></select></label><button className="primary-button">Add contestant</button></form></article><article className="admin-card"><h2>New challenge</h2><form action={saveChallenge} className="admin-form"><label>Name<input name="name" required/></label><div className="form-row"><label>Start<input name="start_date" type="date" required/></label><label>End<input name="end_date" type="date" required/></label></div><label>Starting balance<input name="starting_balance" type="number" step=".01" required/></label><button className="orange-button">Start challenge</button></form></article><article className="admin-card"><h2>Publish update</h2><form action={addContent} className="admin-form"><label>Type<select name="type"><option>Lesson</option><option>Result</option><option>Activity</option></select></label><label>Title<input name="title" required/></label><label>Description<textarea name="description" rows={4} required/></label><button className="primary-button">Publish</button></form></article></div><div className="admin-card contestant-manager"><h2>Contestants</h2>{list?.length?list.map(c=><div className="manage-row" key={c.id}><div><strong>{c.name}</strong><small>{c.broker}</small></div><form action={updateContestant}><input type="hidden" name="id" value={c.id}/><input name="current_balance" type="number" step=".01" defaultValue={c.current_balance}/><select name="status" defaultValue={c.status}><option>Active</option><option>Completed</option><option>Eliminated</option></select><button className="primary-button">Save</button></form><form action={removeContestant}><input type="hidden" name="id" value={c.id}/><button className="remove-button">Remove</button></form></div>):<p>No contestants yet.</p>}</div></div></section>}
